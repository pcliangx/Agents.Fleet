// PLACEHOLDER versions — frozen by #15 (SupportedPlatformMatrix) and #16 (RuntimeLimitProfile).
// Do not consume any of these as canonical until those tickets freeze the values.

export const PROTOCOL_VERSIONS = [1] as const;
export const PLACEHOLDER_PLATFORM_MATRIX_VERSION = 0 as const;
export const PLACEHOLDER_RUNTIME_LIMIT_PROFILE_VERSION = 0 as const;
